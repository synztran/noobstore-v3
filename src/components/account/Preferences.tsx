import React from "react";

const Preferences: React.FC = () => {
	return (
		<div className="p-4">
			<h2 className="font-bold text-lg mb-2">Cài đặt & ưu tiên</h2>
			<div>Hiển thị cài đặt tài khoản, thông báo ở đây.</div>
		</div>
	);
};

export default Preferences;
