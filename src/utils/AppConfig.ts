export const AppConfig = {
  site_name: 'NoobStore',
  title: 'Noob Store',
  description: 'NoobStore belong to Noob Team',
  locale: 'en',
};


export function classNames(...classes: string[]) {
	return classes.filter(Boolean).join(" ");
}